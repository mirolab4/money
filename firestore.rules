rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // السماح بالقراءة والكتابة فقط للمستخدمين المصادق عليهم
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
